[{"id":"","heading":"Overview","text":"Whitening applies a linear map $W$ so that the transformed data has identity covariance. PCA whitening rotates into the eigenbasis and rescales. ZCA whitening rotates back, which keeps the data visually close to the original."},{"id":"goal","heading":"Goal","text":"Let $x \\in \\mathbb{R}^d$ with mean $\\mu$ and covariance $\\Sigma$. Whitening finds $W$ such that\n\n$$\nz = W(x-\\mu),\n\\qquad\n\\operatorname{Cov}(z) = I .\n$$\n\nThe result has unit variance in every direction and zero correlation between features."},{"id":"the-eigendecomposition-solution","heading":"The eigendecomposition solution","text":"Write the eigendecomposition\n\n$$\n\\Sigma = U\\Lambda U^\\top ,\n$$\n\nwhere $U$ is orthogonal and $\\Lambda = \\operatorname{diag}(\\lambda_1,\\dots,\\lambda_d)$ with $\\lambda_j > 0$.\n\nPCA whitening uses\n\n$$\nW_{\\mathrm{PCA}} = \\Lambda^{-1/2}U^\\top .\n$$\n\nZCA whitening uses\n\n$$\nW_{\\mathrm{ZCA}} = U\\Lambda^{-1/2}U^\\top = \\Sigma^{-1/2}.\n$$\n\nLet $z = W(x-\\mu)$ with $W = \\Lambda^{-1/2}U^\\top$.\n\nThe covariance of a linear map is\n\n$$\n\\operatorname{Cov}(z) = W\\Sigma W^\\top .\n$$\n\nSubstitute $\\Sigma = U\\Lambda U^\\top$:\n\n$$\n\\operatorname{Cov}(z)\n=\n\\Lambda^{-1/2}U^\\top\\, U\\Lambda U^\\top\\, U\\Lambda^{-1/2}.\n$$\n\nSince $U$ is orthogonal, $U^\\top U = I$:\n\n$$\n=\n\\Lambda^{-1/2}\\Lambda\\Lambda^{-1/2}\n=\n\\Lambda^{-1/2}\\Lambda^{1/2}\\Lambda^{1/2}\\Lambda^{-1/2}\n=\nI .\n$$\n\nThe same computation with $W_{\\mathrm{ZCA}} = U\\Lambda^{-1/2}U^\\top$ gives\n\n$$\nU\\Lambda^{-1/2}U^\\top U\\Lambda U^\\top U\\Lambda^{-1/2}U^\\top\n=\nU\\Lambda^{-1/2}\\Lambda\\Lambda^{-1/2}U^\\top\n=\nUU^\\top\n=\nI .\n$$"},{"id":"whitening-is-not-unique","heading":"Whitening is not unique","text":"If $W$ whitens, then so does $RW$ for any orthogonal $R$, because\n\n$$\n(RW)\\Sigma(RW)^\\top = R I R^\\top = I .\n$$\n\nSo the whitening transform is defined only up to a rotation.\n\n| Choice | Formula | Property |\n|---|---|---|\n| PCA | $\\Lambda^{-1/2}U^\\top$ | Axes align with principal components |\n| ZCA | $U\\Lambda^{-1/2}U^\\top$ | Closest to the identity in Frobenius norm |\n| Cholesky | $L^{-1}$ with $\\Sigma = LL^\\top$ | Triangular, cheap |\n\nZCA is the standard choice for image data because it preserves spatial structure and looks like local edge enhancement."},{"id":"numerical-stability","heading":"Numerical stability","text":"Small eigenvalues make $\\lambda^{-1/2}$ huge and amplify noise.\n\nTwo standard fixes exist.\n\n1. Add a regularizer:\n\n$$\nW = (\\Sigma + \\epsilon I)^{-1/2}.\n$$\n\n2. Drop directions with tiny eigenvalues. This is whitening plus dimensionality reduction.\n\nAlways fit $\\mu$, $U$, and $\\Lambda$ on the training set only. Applying the same transform to validation and test data prevents leakage."},{"id":"why-whitening-helps-optimization","heading":"Why whitening helps optimization","text":"For a linear model with squared loss, the Hessian is proportional to the input covariance:\n\n$$\nH = \\frac{2}{n} X^\\top X .\n$$\n\nWhitening makes $X^\\top X \\propto I$, so the condition number becomes one. Gradient descent then converges in a single well scaled step direction.\n\nThis is the same motivation behind batch normalization, which whitens per feature within a mini-batch but skips the expensive decorrelation across features."},{"id":"standardization-compared-with-whitening","heading":"Standardization compared with whitening","text":"| Operation | Effect on covariance |\n|---|---|\n| Centering | Mean becomes zero, covariance unchanged |\n| Standardization | Diagonal becomes one, off-diagonal correlations remain |\n| Whitening | Full covariance becomes the identity |\n\nStandardization costs $O(d)$. Whitening costs $O(d^3)$ for the decomposition, so it is impractical for very large $d$."},{"id":"code-example","heading":"Code example","text":"```python\n\ndef fit_whitening(X, eps=1e-5, mode=\"zca\"):\n    mu = X.mean(axis=0)\n    Xc = X - mu\n    cov = (Xc.T @ Xc) / (len(X) - 1)\n    lam, U = np.linalg.eigh(cov)\n    inv_sqrt = np.diag(1.0 / np.sqrt(lam + eps))\n    W = inv_sqrt @ U.T\n    if mode == \"zca\":\n        W = U @ W\n    return mu, W\n\nrng = np.random.default_rng(0)\nA = rng.normal(size=(3, 3))\nX = rng.normal(size=(2000, 3)) @ A.T\n\nmu, W = fit_whitening(X)\nZ = (X - mu) @ W.T\nprint(np.round(np.cov(Z, rowvar=False), 3))\n```\n\nThe printed covariance should be close to the identity."},{"id":"common-mistakes","heading":"Common mistakes","text":"Do not fit the whitening matrix on the full dataset including the test split.\n\nDo not omit the epsilon term. Near zero eigenvalues will explode.\n\nDo not confuse standardization with whitening. Standardization leaves correlations in place."}]