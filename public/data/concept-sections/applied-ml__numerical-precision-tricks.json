[{"id":"","heading":"Overview","text":"Numerical precision tricks avoid overflow, underflow, and cancellation. Common tricks include log-sum-exp, stable softmax, epsilons, and fp32 accumulation."},{"id":"core-idea","heading":"Core idea","text":"Floating point arithmetic has finite range and finite precision.\r\n\r\nNaive formulas can overflow, underflow, or lose small differences.\r\n\r\nStable formulas compute the same mathematical value in a safer way.\r\n\r\nSee Floating Point Representation."},{"id":"stable-softmax","heading":"Stable softmax","text":"Naive softmax computes:\r\n\r\n$$\r\n\\operatorname{softmax}(x_i)\r\n=\r\n\\frac{\\exp(x_i)}{\\sum_j \\exp(x_j)}.\r\n$$\r\n\r\nLarge $x_i$ can make $\\exp(x_i)$ overflow.\r\n\r\nSubtract the maximum logit $m=\\max_j x_j$:\r\n\r\n$$\r\n\\operatorname{softmax}(x_i)\r\n=\r\n\\frac{\\exp(x_i-m)}{\\sum_j \\exp(x_j-m)}.\r\n$$\r\n\r\nThis gives the same result and avoids large exponentials.\r\n\r\n\r\nLet $c$ be any constant.\r\n\r\n$$\r\n\\frac{\\exp(x_i-c)}{\\sum_j \\exp(x_j-c)}\r\n=\r\n\\frac{\\exp(x_i)\\exp(-c)}\r\n{\\sum_j \\exp(x_j)\\exp(-c)}.\r\n$$\r\n\r\nThe factor $\\exp(-c)$ appears in the numerator and denominator.\r\n\r\nIt cancels out.\r\n\r\nThus, subtracting $c$ does not change softmax."},{"id":"log-sum-exp","heading":"Log-sum-exp","text":"Use:\r\n\r\n$$\r\n\\log \\sum_i \\exp(x_i)\r\n=\r\nm + \\log \\sum_i \\exp(x_i-m).\r\n$$\r\n\r\nThis is stable when $m=\\max_i x_i$."},{"id":"other-tricks","heading":"Other tricks","text":"Add a small $\\epsilon$ before division or logarithms.\r\n\r\nUse `log1p(x)` for $\\log(1+x)$ when $x$ is small.\r\n\r\nUse `expm1(x)` for $\\exp(x)-1$ when $x$ is small.\r\n\r\nAccumulate reductions in fp32 during Mixed Precision Training."},{"id":"code-example","heading":"Code example","text":"```python\r\n\r\n\r\ndef stable_softmax(x, dim=-1):\r\n    z = x - x.max(dim=dim, keepdim=True).values\r\n    return torch.exp(z) / torch.exp(z).sum(dim=dim, keepdim=True)\r\n```"},{"id":"common-mistakes","heading":"Common mistakes","text":"Do not compute softmax before cross entropy if the library expects logits.\r\n\r\nDo not take `log(softmax(x))` manually.\r\n\r\nUse a fused stable cross-entropy function when possible."}]