[{"id":"","heading":"Overview","text":"PPO is an on-policy actor-critic method. It uses clipped policy ratios so the new policy does not move too far from the data policy."},{"id":"core-idea","heading":"Core idea","text":"PPO collects rollouts with an old policy.\r\n\r\nIt updates a new policy with probability ratio:\r\n\r\n$$\r\nr_t(\\theta)\r\n=\r\n\\frac{\\pi_\\theta(a_t \\mid s_t)}\r\n{\\pi_{\\theta_{\\mathrm{old}}}(a_t \\mid s_t)}.\r\n$$\r\n\r\nThe clipped surrogate objective is:\r\n\r\n$$\r\nL^{\\mathrm{CLIP}}(\\theta)\r\n=\r\n\\mathbb{E}\r\n\\left[\r\n\\min\r\n\\left(\r\nr_t(\\theta)\\hat A_t,\r\n\\operatorname{clip}(r_t(\\theta),1-\\epsilon,1+\\epsilon)\\hat A_t\r\n\\right)\r\n\\right].\r\n$$"},{"id":"why-clipping-helps","heading":"Why clipping helps","text":"Large policy updates can destroy performance.\r\n\r\nThe ratio measures how much action probability changes.\r\n\r\nClipping limits the objective incentive outside a trust region.\r\n\r\nIt is not a hard KL constraint.\r\n\r\nMany implementations also monitor KL divergence."},{"id":"ppo-training-loop","heading":"PPO training loop","text":"1. Collect rollouts with the current policy.\r\n\r\n2. Estimate advantages, often with GAE.\r\n\r\n3. Optimize the clipped policy loss for several epochs.\r\n\r\n4. Train the value function.\r\n\r\n5. Repeat with fresh rollouts."},{"id":"code-example","heading":"Code example","text":"```python\r\n\r\n\r\nratio = torch.exp(logp_new - logp_old)\r\nunclipped = ratio * adv\r\nclipped = torch.clamp(ratio, 1 - eps, 1 + eps) * adv\r\npolicy_loss = -torch.mean(torch.minimum(unclipped, clipped))\r\n```"},{"id":"common-mistakes","heading":"Common mistakes","text":"Do not reuse old rollouts for too many epochs.\r\n\r\nDo not forget to detach old log probabilities.\r\n\r\nDo not treat clipping as a full off-policy correction."}]