[{"id":"","heading":"Overview","text":"An RNN applies the same cell at each time step and carries a hidden state. Backpropagation through time multiplies many Jacobians, so gradients vanish or explode. The LSTM adds an additive cell state and gates, which keeps a near constant gradient path."},{"id":"vanilla-rnn","heading":"Vanilla RNN","text":"$$\r\nh_t = \\tanh\\big(W_{hh}h_{t-1} + W_{xh}x_t + b_h\\big),\r\n\\qquad\r\ny_t = W_{hy}h_t + b_y .\r\n$$\r\n\r\nThe same weights apply at every step. The parameter count does not grow with sequence length.\r\n\r\nThe hidden state is a lossy summary of everything seen so far."},{"id":"backpropagation-through-time","heading":"Backpropagation through time","text":"Unroll the network and apply the chain rule. The gradient of the loss at step $T$ with respect to the state at step $t$ is\r\n\r\n$$\r\n\\frac{\\partial h_T}{\\partial h_t}\r\n=\r\n\\prod_{s=t+1}^{T}\\frac{\\partial h_s}{\\partial h_{s-1}}\r\n=\r\n\\prod_{s=t+1}^{T} D_s W_{hh}^\\top ,\r\n$$\r\n\r\nwhere $D_s = \\operatorname{diag}\\big(1 - \\tanh^2(\\cdot)\\big)$."},{"id":"vanishing-and-exploding-gradients","heading":"Vanishing and exploding gradients","text":"Take the spectral norm of the product:\r\n\r\n$$\r\n\\left\\|\\frac{\\partial h_T}{\\partial h_t}\\right\\|\r\n\\le\r\n\\prod_{s=t+1}^{T}\\|D_s\\|\\,\\|W_{hh}\\| .\r\n$$\r\n\r\nFor the $\\tanh$ nonlinearity, $\\tanh'(z) = 1-\\tanh^2(z) \\in (0,1]$, so $\\|D_s\\| \\le 1$.\r\n\r\nLet $\\gamma = \\|W_{hh}\\|$ and let $\\delta = \\max_s \\|D_s\\| \\le 1$. Then\r\n\r\n$$\r\n\\left\\|\\frac{\\partial h_T}{\\partial h_t}\\right\\|\r\n\\le\r\n(\\gamma\\delta)^{T-t}.\r\n$$\r\n\r\nIf $\\gamma\\delta < 1$, the bound decays geometrically in the time gap. The gradient vanishes.\r\n\r\nA matching lower bound holds with the smallest singular value. If the smallest singular value of $W_{hh}$ times the smallest diagonal entry exceeds one, the gradient grows geometrically. It explodes.\r\n\r\nOnly $\\gamma\\delta = 1$ exactly avoids both, and that condition is not stable during training.\r\n\r\n\r\nConsequences:\r\n\r\n- Vanishing gradients block learning of long range dependencies. The model still trains, but only on short spans.\r\n- Exploding gradients produce NaN values and sudden loss spikes. Gradient clipping fixes this cheaply.\r\n\r\nTruncated BPTT limits the unroll length to $k$ steps to bound memory and compute."},{"id":"lstm","heading":"LSTM","text":"The LSTM adds a cell state $c_t$ with an additive update.\r\n\r\n$$\r\nf_t = \\sigma(W_f[h_{t-1}, x_t] + b_f) \\quad \\text{forget gate}\r\n$$\r\n\r\n$$\r\ni_t = \\sigma(W_i[h_{t-1}, x_t] + b_i) \\quad \\text{input gate}\r\n$$\r\n\r\n$$\r\n\\tilde{c}_t = \\tanh(W_c[h_{t-1}, x_t] + b_c) \\quad \\text{candidate}\r\n$$\r\n\r\n$$\r\nc_t = f_t \\odot c_{t-1} + i_t \\odot \\tilde{c}_t\r\n$$\r\n\r\n$$\r\no_t = \\sigma(W_o[h_{t-1}, x_t] + b_o) \\quad \\text{output gate}\r\n$$\r\n\r\n$$\r\nh_t = o_t \\odot \\tanh(c_t)\r\n$$"},{"id":"why-the-lstm-helps","heading":"Why the LSTM helps","text":"The cell state update is additive, not multiplicative through a weight matrix. The Jacobian along the cell path is\r\n\r\n$$\r\n\\frac{\\partial c_t}{\\partial c_{t-1}} = \\operatorname{diag}(f_t) + (\\text{gate terms}).\r\n$$\r\n\r\nWhen the forget gate stays near one, the derivative stays near one. Gradients then flow across many steps with little decay. This is called the constant error carousel.\r\n\r\nInitialize the forget gate bias to a positive value, often 1.0. The gate then starts near one and the model remembers by default."},{"id":"gru","heading":"GRU","text":"The GRU merges the forget and input gates.\r\n\r\n$$\r\nz_t = \\sigma(W_z[h_{t-1},x_t]),\r\n\\qquad\r\nr_t = \\sigma(W_r[h_{t-1},x_t]),\r\n$$\r\n\r\n$$\r\n\\tilde{h}_t = \\tanh\\big(W[r_t \\odot h_{t-1}, x_t]\\big),\r\n$$\r\n\r\n$$\r\nh_t = (1-z_t)\\odot h_{t-1} + z_t \\odot \\tilde{h}_t .\r\n$$\r\n\r\nThe GRU has three gate matrices instead of four and no separate cell state. It trains faster and performs about the same on most tasks."},{"id":"parameter-counts","heading":"Parameter counts","text":"For hidden size $H$ and input size $D$:\r\n\r\n| Cell | Parameters |\r\n|---|---|\r\n| Vanilla RNN | $H(H+D) + H$ |\r\n| GRU | $3H(H+D) + 3H$ |\r\n| LSTM | $4H(H+D) + 4H$ |"},{"id":"variants-and-practice","heading":"Variants and practice","text":"- Bidirectional RNNs run one pass forward and one backward, then concatenate. They need the full sequence, so they do not suit streaming.\r\n- Stacked layers increase depth. Add residual connections above two or three layers.\r\n- Dropout should apply to the non-recurrent connections, or use variational dropout with the same mask at every step.\r\n- Layer normalization inside the cell stabilizes training."},{"id":"comparison-with-attention-and-ssms","heading":"Comparison with attention and SSMs","text":"| Property | RNN or LSTM | Transformer | S4 |\r\n|---|---|---|---|\r\n| Training parallel over time | No | Yes | Yes |\r\n| Inference memory per step | $O(H)$ constant | $O(T)$ KV cache | $O(H)$ constant |\r\n| Path length between distant tokens | $O(T)$ | $O(1)$ | $O(1)$ through the kernel |\r\n| Sequence length cost | $O(T)$ | $O(T^2)$ | $O(T\\log T)$ |\r\n\r\nThe sequential dependency is the main reason transformers replaced RNNs for large scale training. State space models recovered parallel training while keeping constant state at inference."},{"id":"code-example","heading":"Code example","text":"```python\r\n\r\n\r\ndef lstm_step(x, h, c, W, b):\r\n    \"\"\"W: (4H, H + D), b: (4H,). Gate order: i, f, g, o.\"\"\"\r\n    H = h.shape[0]\r\n    z = W @ np.concatenate([h, x]) + b\r\n    i = 1 / (1 + np.exp(-z[0:H]))\r\n    f = 1 / (1 + np.exp(-z[H:2 * H]))\r\n    g = np.tanh(z[2 * H:3 * H])\r\n    o = 1 / (1 + np.exp(-z[3 * H:4 * H]))\r\n    c_new = f * c + i * g\r\n    h_new = o * np.tanh(c_new)\r\n    return h_new, c_new\r\n\r\nrng = np.random.default_rng(0)\r\nH, D = 4, 3\r\nW = rng.normal(size=(4 * H, H + D)) * 0.1\r\nb = np.zeros(4 * H)\r\nb[H:2 * H] = 1.0        # positive forget gate bias\r\nh, c = np.zeros(H), np.zeros(H)\r\nfor t in range(5):\r\n    h, c = lstm_step(rng.normal(size=D), h, c, W, b)\r\nprint(np.round(h, 3))\r\n```"},{"id":"common-mistakes","heading":"Common mistakes","text":"Do not clip gradients to fix vanishing gradients. Clipping only handles explosion.\r\n\r\nDo not apply standard dropout to the recurrent connection with a fresh mask each step. It destroys the memory.\r\n\r\nDo not forget to reset the hidden state between independent sequences.\r\n\r\nDo not use a bidirectional model for a task that must run online."}]