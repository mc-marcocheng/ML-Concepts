[{"id":"","heading":"Overview","text":"k-NN stores the training set and predicts by majority vote or average over the k nearest points. Small k gives low bias and high variance. The method needs feature scaling and it degrades in high dimensions."},{"id":"algorithm","heading":"Algorithm","text":"For a query $x$:\r\n\r\n1. Compute the distance from $x$ to every training point.\r\n2. Take the $k$ points with the smallest distance.\r\n3. Classification returns the majority label. Regression returns the mean target.\r\n\r\nThere is no fitted parameter. All work happens at prediction time. This is called a lazy or memory based method."},{"id":"distance-metrics","heading":"Distance metrics","text":"| Metric | Formula | Use |\r\n|---|---|---|\r\n| Euclidean | $\\|x-x'\\|_2$ | Continuous features on the same scale |\r\n| Manhattan | $\\|x-x'\\|_1$ | High dimension, robust to outliers |\r\n| Minkowski | $\\left(\\sum_j |x_j-x'_j|^p\\right)^{1/p}$ | General family |\r\n| Cosine | $1 - \\frac{x^\\top x'}{\\|x\\|\\|x'\\|}$ | Text and embeddings |\r\n| Hamming | Count of differing entries | Categorical features |\r\n\r\nFeature scaling is mandatory for Euclidean distance. A feature measured in meters and a feature measured in millimeters do not contribute equally."},{"id":"choosing-k","heading":"Choosing k","text":"$k$ controls smoothness.\r\n\r\n| $k$ | Bias | Variance | Boundary |\r\n|---|---|---|---|\r\n| 1 | Low | High | Very jagged |\r\n| Moderate | Balanced | Balanced | Smooth |\r\n| $n$ | High | Low | Constant prediction |\r\n\r\nPick $k$ with cross validation. For binary classification an odd $k$ avoids ties.\r\n\r\nThe effective number of parameters is about $n/k$."},{"id":"distance-weighting","heading":"Distance weighting","text":"Uniform voting treats all $k$ neighbors equally. Distance weighting gives closer points more influence:\r\n\r\n$$\r\n\\hat{y}(x)\r\n=\r\n\\frac{\\sum_{i \\in N_k(x)} w_i y_i}{\\sum_{i \\in N_k(x)} w_i},\r\n\\qquad\r\nw_i = \\frac{1}{\\|x - x_i\\| + \\epsilon}.\r\n$$\r\n\r\nThis softens the effect of a large $k$."},{"id":"error-bound-for-1-nn","heading":"Error bound for 1-NN","text":"The asymptotic error of 1-NN is at most twice the Bayes error.\r\n\r\n\r\nConsider binary classification. Let $\\eta(x) = p(y=1\\mid x)$. The Bayes error at $x$ is\r\n\r\n$$\r\ne^\\star(x) = \\min\\big(\\eta(x), 1-\\eta(x)\\big).\r\n$$\r\n\r\nAs $n \\to \\infty$, the nearest neighbor $x'$ of $x$ converges to $x$ under mild continuity conditions. So $\\eta(x') \\to \\eta(x)$.\r\n\r\nThe 1-NN rule copies the label of $x'$. An error occurs when the true label of $x$ differs from the label of $x'$. Since the two labels are drawn independently given the same $\\eta$,\r\n\r\n$$\r\ne_{1\\mathrm{NN}}(x)\r\n=\r\n\\eta(x)\\big(1-\\eta(x)\\big) + \\big(1-\\eta(x)\\big)\\eta(x)\r\n=\r\n2\\eta(x)\\big(1-\\eta(x)\\big).\r\n$$\r\n\r\nWrite $e^\\star = \\min(\\eta, 1-\\eta)$. Then $\\max(\\eta, 1-\\eta) = 1-e^\\star$ and\r\n\r\n$$\r\ne_{1\\mathrm{NN}}\r\n=\r\n2 e^\\star (1 - e^\\star)\r\n\\le\r\n2 e^\\star .\r\n$$\r\n\r\nAveraging over $x$ preserves the bound.\r\n\r\n\r\nSo a very simple rule is never worse than twice the best possible error, in the large sample limit."},{"id":"computational-cost","heading":"Computational cost","text":"Brute force prediction costs $O(nd)$ per query. For large $n$ this is slow.\r\n\r\n| Structure | Best case | Weakness |\r\n|---|---|---|\r\n| KD-tree | $O(\\log n)$ per query | Degrades above roughly 20 dimensions |\r\n| Ball tree | Better for higher $d$ | Still limited |\r\n| LSH | Sublinear approximate | Approximate results |\r\n| HNSW graph | Fast approximate | Extra memory |\r\n\r\nMemory cost is $O(nd)$ because the whole training set stays in memory."},{"id":"high-dimensions","heading":"High dimensions","text":"Distance concentration weakens the notion of a nearest neighbor. See curse of dimensionality.\r\n\r\nMitigations include dimensionality reduction, metric learning, and feature selection."},{"id":"code-example","heading":"Code example","text":"```python\r\n\r\n\r\ndef knn_predict(X_train, y_train, X_query, k=5):\r\n    d = ((X_query[:, None, :] - X_train[None, :, :]) ** 2).sum(-1)\r\n    idx = np.argsort(d, axis=1)[:, :k]\r\n    votes = y_train[idx]\r\n    return np.array([np.bincount(v).argmax() for v in votes])\r\n\r\nrng = np.random.default_rng(0)\r\nX = rng.normal(size=(300, 4))\r\ny = (X[:, 0] + X[:, 1] > 0).astype(int)\r\npred = knn_predict(X[:250], y[:250], X[250:], k=5)\r\nprint((pred == y[250:]).mean())\r\n```\r\n\r\nNote the `np.argsort` call costs $O(n \\log n)$. Use `np.argpartition` for speed."},{"id":"common-mistakes","heading":"Common mistakes","text":"Do not skip feature standardization.\r\n\r\nDo not include the query point itself when you evaluate on training data. That gives a false perfect score at $k=1$.\r\n\r\nDo not use a small $k$ on noisy data. Noise then dominates the vote."}]