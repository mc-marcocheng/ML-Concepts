[{"id":"","heading":"Overview","text":"A support vector machine maximizes the distance from the decision boundary to the closest points. The soft margin form is a convex quadratic program equal to hinge loss plus L2 regularization. The dual depends on data only through inner products, which allows kernels."},{"id":"margin","heading":"Margin","text":"Take labels $y_i \\in \\{-1,+1\\}$ and a hyperplane $w^\\top x + b = 0$.\r\n\r\nThe signed distance from $x_i$ to the hyperplane is\r\n\r\n$$\r\n\\frac{y_i(w^\\top x_i + b)}{\\|w\\|}.\r\n$$\r\n\r\nFix the scale so the closest points satisfy $y_i(w^\\top x_i + b) = 1$. This is the canonical form.\r\n\r\n\r\nLet $x_+$ satisfy $w^\\top x_+ + b = 1$ and let $x_-$ satisfy $w^\\top x_- + b = -1$.\r\n\r\nSubtract the two equations:\r\n\r\n$$\r\nw^\\top (x_+ - x_-) = 2 .\r\n$$\r\n\r\nThe distance between the two supporting hyperplanes is the length of the projection of $x_+ - x_-$ onto the unit normal $w/\\|w\\|$:\r\n\r\n$$\r\n\\frac{w^\\top (x_+ - x_-)}{\\|w\\|}\r\n=\r\n\\frac{2}{\\|w\\|}.\r\n$$\r\n\r\nSo maximizing the margin $2/\\|w\\|$ is the same as minimizing $\\tfrac{1}{2}\\|w\\|^2$."},{"id":"hard-margin-primal","heading":"Hard margin primal","text":"$$\r\n\\min_{w,b}\\ \\tfrac{1}{2}\\|w\\|^2\r\n\\quad \\text{subject to} \\quad\r\ny_i(w^\\top x_i + b) \\ge 1 \\ \\ \\forall i .\r\n$$\r\n\r\nThis is a convex quadratic program. It has no solution when the data is not linearly separable."},{"id":"soft-margin-primal","heading":"Soft margin primal","text":"Add slack variables $\\xi_i \\ge 0$:\r\n\r\n$$\r\n\\min_{w,b,\\xi}\\ \\tfrac{1}{2}\\|w\\|^2 + C\\sum_{i=1}^{n}\\xi_i\r\n\\quad \\text{subject to} \\quad\r\ny_i(w^\\top x_i + b) \\ge 1 - \\xi_i,\\ \\ \\xi_i \\ge 0 .\r\n$$\r\n\r\nAt the optimum $\\xi_i = \\max(0, 1 - y_i(w^\\top x_i + b))$. So the problem is equivalent to\r\n\r\n$$\r\n\\min_{w,b}\\ \\sum_{i=1}^{n}\\max\\big(0,\\ 1 - y_i(w^\\top x_i+b)\\big)\r\n+ \\frac{1}{2C}\\|w\\|^2 .\r\n$$\r\n\r\nThis is hinge loss plus L2 regularization.\r\n\r\n$C$ controls the tradeoff. A large $C$ punishes violations hard and gives a narrow margin. A small $C$ allows violations and gives a wide margin."},{"id":"dual-form","heading":"Dual form","text":"Introduce multipliers $\\alpha_i \\ge 0$. The dual is\r\n\r\n$$\r\n\\max_{\\alpha}\\ \\sum_{i=1}^{n}\\alpha_i\r\n-\\tfrac{1}{2}\\sum_{i,j}\\alpha_i\\alpha_j y_i y_j\\, x_i^\\top x_j\r\n$$\r\n\r\nsubject to\r\n\r\n$$\r\n0 \\le \\alpha_i \\le C,\r\n\\qquad\r\n\\sum_{i=1}^{n}\\alpha_i y_i = 0 .\r\n$$\r\n\r\nThe weight vector is\r\n\r\n$$\r\nw = \\sum_{i=1}^{n}\\alpha_i y_i x_i .\r\n$$\r\n\r\nTwo facts follow.\r\n\r\n1. The data enters only through inner products $x_i^\\top x_j$.\r\n2. Only points with $\\alpha_i > 0$ contribute. These are the support vectors."},{"id":"kkt-conditions","heading":"KKT conditions","text":"| Condition | Meaning |\r\n|---|---|\r\n| $\\alpha_i = 0$ | Point is outside the margin and correctly classified |\r\n| $0 < \\alpha_i < C$ | Point sits exactly on the margin |\r\n| $\\alpha_i = C$ | Point violates the margin or is misclassified |\r\n\r\nRemoving a non-support vector does not change the solution."},{"id":"kernels","heading":"Kernels","text":"Replace the inner product with a kernel $k(x,x') = \\phi(x)^\\top\\phi(x')$. The dual becomes\r\n\r\n$$\r\n\\max_\\alpha \\sum_i \\alpha_i - \\tfrac{1}{2}\\sum_{i,j}\\alpha_i\\alpha_j y_i y_j\\, k(x_i,x_j).\r\n$$\r\n\r\nThe prediction is\r\n\r\n$$\r\nf(x) = \\sum_{i}\\alpha_i y_i\\, k(x_i, x) + b .\r\n$$\r\n\r\nYou never compute $\\phi$ explicitly. This is the kernel trick.\r\n\r\n| Kernel | Formula |\r\n|---|---|\r\n| Linear | $x^\\top x'$ |\r\n| Polynomial | $(\\gamma x^\\top x' + r)^p$ |\r\n| RBF | $\\exp(-\\gamma\\|x-x'\\|^2)$ |\r\n| Sigmoid | $\\tanh(\\gamma x^\\top x' + r)$ |\r\n\r\nA valid kernel produces a positive semi-definite Gram matrix. This is Mercer's condition.\r\n\r\nThe RBF kernel maps to an infinite dimensional space. A large $\\gamma$ makes each point influence only its immediate area, which risks overfitting."},{"id":"regression-and-multiclass","heading":"Regression and multiclass","text":"Support vector regression uses an $\\epsilon$-insensitive loss. Errors smaller than $\\epsilon$ cost nothing.\r\n\r\nSVMs are binary by design. Multiclass uses one-vs-rest or one-vs-one."},{"id":"practical-notes","heading":"Practical notes","text":"- Scale the features. The RBF kernel and the regularizer both depend on scale.\r\n- Tune $C$ and $\\gamma$ together on a log grid with cross validation.\r\n- Training cost is between $O(n^2)$ and $O(n^3)$ for kernel SVMs, so they suit medium sized data.\r\n- A linear SVM on large sparse data trains fast with `LinearSVC` or SGD on the hinge loss."},{"id":"code-example","heading":"Code example","text":"```python\r\n\r\n\r\ndef hinge_sgd(X, y, C=1.0, lr=0.01, epochs=200, seed=0):\r\n    \"\"\"y in {-1, +1}. Minimizes 0.5||w||^2 + C * sum(hinge).\"\"\"\r\n    rng = np.random.default_rng(seed)\r\n    n, d = X.shape\r\n    w = np.zeros(d)\r\n    b = 0.0\r\n    for _ in range(epochs):\r\n        for i in rng.permutation(n):\r\n            margin = y[i] * (X[i] @ w + b)\r\n            if margin < 1:\r\n                w = w - lr * (w - C * y[i] * X[i])\r\n                b = b + lr * C * y[i]\r\n            else:\r\n                w = w - lr * w\r\n    return w, b\r\n\r\nrng = np.random.default_rng(0)\r\nX = rng.normal(size=(200, 2))\r\ny = np.where(X[:, 0] + X[:, 1] > 0, 1, -1)\r\nw, b = hinge_sgd(X, y)\r\nprint(np.mean(np.sign(X @ w + b) == y))\r\n```"},{"id":"common-mistakes","heading":"Common mistakes","text":"Do not forget that SVM labels are $-1$ and $+1$, not $0$ and $1$.\r\n\r\nDo not use an RBF kernel on unscaled features.\r\n\r\nDo not read $C$ as a regularization strength. A large $C$ means weak regularization."}]