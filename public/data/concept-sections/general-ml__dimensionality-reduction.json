[{"id":"","heading":"Overview","text":"PCA finds the orthogonal directions of maximum variance and equals the top singular vectors of the centered data. Random projection preserves distances with high probability. t-SNE and UMAP are visualization tools, not general feature extractors."},{"id":"why-reduce-dimension","heading":"Why reduce dimension","text":"- Fight the curse of dimensionality.\r\n- Remove correlated or noisy features.\r\n- Cut storage and compute cost.\r\n- Enable two dimensional visualization.\r\n- Decorrelate inputs before a linear model."},{"id":"pca","heading":"PCA","text":"Center the data so that $\\sum_i x_i = 0$. Let $\\Sigma = \\frac{1}{n}X^\\top X$ be the covariance.\r\n\r\nPCA finds orthonormal directions $w_1, w_2, \\dots$ that maximize the projected variance.\r\n\r\n\r\nMaximize the projected variance subject to a unit norm constraint:\r\n\r\n$$\r\n\\max_{w}\\ w^\\top \\Sigma w\r\n\\quad\\text{subject to}\\quad\r\nw^\\top w = 1 .\r\n$$\r\n\r\nForm the Lagrangian:\r\n\r\n$$\r\n\\mathcal{L}(w,\\lambda) = w^\\top \\Sigma w - \\lambda(w^\\top w - 1).\r\n$$\r\n\r\nTake the gradient with respect to $w$:\r\n\r\n$$\r\n\\nabla_w \\mathcal{L} = 2\\Sigma w - 2\\lambda w = 0 .\r\n$$\r\n\r\nSo\r\n\r\n$$\r\n\\Sigma w = \\lambda w .\r\n$$\r\n\r\nThe solution is an eigenvector of $\\Sigma$. Substituting back gives the objective value\r\n\r\n$$\r\nw^\\top \\Sigma w = \\lambda .\r\n$$\r\n\r\nTo maximize, pick the eigenvector with the largest eigenvalue.\r\n\r\nFor the second component, add the constraint $w_2^\\top w_1 = 0$. The same argument gives the eigenvector with the second largest eigenvalue. Induction gives the full ordered set."},{"id":"equivalent-views","heading":"Equivalent views","text":"PCA has three equivalent characterizations.\r\n\r\n1. Maximum variance projection.\r\n2. Minimum squared reconstruction error over rank $k$ linear subspaces.\r\n3. Truncated singular value decomposition of the centered data matrix.\r\n\r\nFor the SVD view, write $X = U S V^\\top$. The principal directions are the columns of $V$, and the projected coordinates are $US$. Computing the SVD directly is more stable than forming $X^\\top X$."},{"id":"explained-variance","heading":"Explained variance","text":"The fraction of variance kept by $k$ components is\r\n\r\n$$\r\n\\frac{\\sum_{j=1}^{k}\\lambda_j}{\\sum_{j=1}^{d}\\lambda_j}.\r\n$$\r\n\r\nChoose $k$ from a scree plot or from a target such as 95 percent."},{"id":"limits","heading":"Limits","text":"- PCA is linear. It cannot unfold a curved manifold.\r\n- PCA is scale sensitive. Standardize features with different units.\r\n- Directions of large variance are not always directions of large class separation.\r\n- The components are dense combinations of all features, so they are hard to read."},{"id":"linear-discriminant-analysis","heading":"Linear discriminant analysis","text":"LDA is supervised. It maximizes the ratio of between class scatter to within class scatter:\r\n\r\n$$\r\n\\max_W \\frac{|W^\\top S_B W|}{|W^\\top S_W W|}.\r\n$$\r\n\r\nThe solution comes from the eigenvectors of $S_W^{-1}S_B$. LDA gives at most $K-1$ components for $K$ classes.\r\n\r\nUse LDA when you want a projection tuned for class separation, and PCA when you want a projection that keeps variance."},{"id":"random-projection","heading":"Random projection","text":"The Johnson-Lindenstrauss lemma says the following. For $n$ points and any $\\epsilon \\in (0,1)$, a random linear map into\r\n\r\n$$\r\nk = O\\!\\left(\\frac{\\log n}{\\epsilon^2}\\right)\r\n$$\r\n\r\ndimensions preserves all pairwise distances within a factor $1 \\pm \\epsilon$ with high probability.\r\n\r\nThe target dimension depends on the number of points, not on the original dimension. The map needs no training, which makes it very cheap for streaming data."},{"id":"nonlinear-methods","heading":"Nonlinear methods","text":"| Method | Idea | Note |\r\n|---|---|---|\r\n| Kernel PCA | PCA in a kernel feature space | Needs an $n \\times n$ matrix |\r\n| MDS | Preserve pairwise distances | Classical MDS equals PCA for Euclidean distances |\r\n| Isomap | MDS on geodesic graph distances | Unfolds curved manifolds |\r\n| LLE | Preserve local linear reconstructions | Sensitive to neighborhood size |\r\n| t-SNE | Match neighbor distributions with a heavy tailed target | Visualization only |\r\n| UMAP | Fuzzy topological structure | Faster, better global structure than t-SNE |\r\n| Autoencoder | Learn a nonlinear bottleneck | Scales to large data, parametric |"},{"id":"t-sne-cautions","heading":"t-SNE cautions","text":"t-SNE optimizes a KL divergence between neighbor distributions. Consequences follow.\r\n\r\n- Cluster sizes carry no meaning.\r\n- Distances between clusters carry little meaning.\r\n- The perplexity setting changes the picture a great deal.\r\n- There is no natural way to map new points, because the embedding is not parametric.\r\n\r\nUse t-SNE and UMAP for looking at data. Do not feed their output into a downstream model without care."},{"id":"choosing-a-method","heading":"Choosing a method","text":"1. Need a linear, invertible, fast map. Use PCA.\r\n2. Need class separation. Use LDA.\r\n3. Need a cheap map with distance guarantees. Use random projection.\r\n4. Need a picture. Use UMAP or t-SNE.\r\n5. Need a learned nonlinear map that applies to new data. Use an autoencoder."},{"id":"code-example","heading":"Code example","text":"```python\r\n\r\n\r\ndef pca_fit(X, k):\r\n    mu = X.mean(axis=0)\r\n    Xc = X - mu\r\n    U, S, Vt = np.linalg.svd(Xc, full_matrices=False)\r\n    components = Vt[:k]\r\n    explained = (S ** 2) / (S ** 2).sum()\r\n    return mu, components, explained[:k]\r\n\r\ndef pca_transform(X, mu, components):\r\n    return (X - mu) @ components.T\r\n\r\ndef pca_inverse(Z, mu, components):\r\n    return Z @ components + mu\r\n\r\nrng = np.random.default_rng(0)\r\nbase = rng.normal(size=(200, 2))\r\nX = base @ rng.normal(size=(2, 10)) + 0.01 * rng.normal(size=(200, 10))\r\n\r\nmu, comps, ev = pca_fit(X, k=2)\r\nZ = pca_transform(X, mu, comps)\r\nXr = pca_inverse(Z, mu, comps)\r\nprint(ev.sum(), np.mean((X - Xr) ** 2))\r\n```\r\n\r\nTwo components should explain almost all the variance, because the data has rank two plus noise."},{"id":"common-mistakes","heading":"Common mistakes","text":"Do not run PCA without centering. The first component then points at the mean.\r\n\r\nDo not fit PCA on the training and test sets together.\r\n\r\nDo not read cluster distances in a t-SNE plot as real distances.\r\n\r\nDo not assume high variance means high predictive value."}]