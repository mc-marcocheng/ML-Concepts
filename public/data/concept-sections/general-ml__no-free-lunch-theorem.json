[{"id":"","heading":"Overview","text":"If you average over every possible target function, all algorithms tie. Useful learning therefore depends on assumptions that match the real problem, and those assumptions are called inductive bias."},{"id":"core-idea","heading":"Core idea","text":"Wolpert stated the theorem for supervised learning. Wolpert and Macready stated a matching result for search and optimization.\n\nThe supervised version says this. Take a finite input space $\\mathcal{X}$ and a finite label space $\\mathcal{Y}$. Put a uniform distribution over all functions $f:\\mathcal{X}\\to\\mathcal{Y}$. Then for any two algorithms $A$ and $B$,\n\n$$\n\\mathbb{E}_f\\big[\\mathrm{err}_{\\text{off}}(A, f)\\big]\n=\n\\mathbb{E}_f\\big[\\mathrm{err}_{\\text{off}}(B, f)\\big],\n$$\n\nwhere $\\mathrm{err}_{\\text{off}}$ is the error on inputs outside the training set."},{"id":"why-the-average-ties","heading":"Why the average ties","text":"The proof rests on symmetry. The training set fixes the labels on seen inputs. The uniform prior leaves the labels on unseen inputs completely free.\n\nLet $S$ be a training set that covers a subset $\\mathcal{X}_S \\subset \\mathcal{X}$. Let $x \\notin \\mathcal{X}_S$ be an unseen input.\n\nAn algorithm $A$ produces a hypothesis $h_A$ that depends only on $S$. So the prediction $h_A(x)$ is fixed once $S$ is fixed.\n\nNow sample $f$ uniformly from all functions consistent with $S$. The value $f(x)$ is uniform over $\\mathcal{Y}$ and independent of $f$ on other unseen points.\n\nThe probability of an error at $x$ is\n\n$$\n\\Pr\\big[f(x) \\neq h_A(x)\\big]\n=\n1 - \\frac{1}{|\\mathcal{Y}|}.\n$$\n\nThis value does not depend on $h_A(x)$. So it does not depend on $A$.\n\nAverage over all unseen $x$ and over all training sets. The expected off-training-set error equals $1 - 1/|\\mathcal{Y}|$ for every algorithm."},{"id":"what-the-theorem-does-not-say","heading":"What the theorem does not say","text":"The theorem does not say that all algorithms perform equally on real data. Real problems are not uniform over all functions.\n\nReal targets are smooth, compositional, or sparse far more often than chance would allow.\n\nThe theorem also says nothing about training set error. It concerns unseen inputs only."},{"id":"inductive-bias","heading":"Inductive bias","text":"Inductive bias is the set of assumptions that an algorithm uses to generalize beyond the data.\n\n| Model | Inductive bias |\n|---|---|\n| Linear regression | The target is close to a linear function |\n| k-NN | Nearby inputs share labels |\n| CNN | Local patterns, translation equivariance |\n| Decision tree | Axis aligned piecewise constant structure |\n| Ridge regression | Small weights are more plausible |\n\nThe theorem turns model choice into a claim about the problem. You must pick a bias that fits the data you actually have."},{"id":"practical-consequences","heading":"Practical consequences","text":"1. Compare models with cross validation on your own data. Do not trust a general ranking.\n2. Encode domain structure when you can. Structure beats raw capacity when data is limited.\n3. Expect no single best optimizer, loss, or architecture across all tasks."},{"id":"common-mistakes","heading":"Common mistakes","text":"Do not use the theorem to argue that model choice does not matter. It argues the opposite for real data.\n\nDo not read the theorem as a statement about a specific dataset.\n\nDo not forget that the result assumes a uniform prior over targets."}]