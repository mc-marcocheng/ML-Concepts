- id: is-mcq-1
  type: mcq
  prompt: "What is the importance weight for samples from $q$ used to estimate an expectation under $p$?"
  difficulty: 2
  options:
    - "$q(x)/p(x)$"
    - "$p(x)/q(x)$"
    - "$p(x)q(x)$"
    - "$p(x)-q(x)$"
  correctIndex: 1
  explanation: "The weight converts a $q$ expectation into a $p$ expectation."
  anchor: core-idea
- id: is-latex-1
  type: latex
  prompt: "Write the trajectory importance ratio when dynamics cancel."
  difficulty: 4
  answer: "\\prod_t \\frac{\\pi(a_t \\mid s_t)}{\\mu(a_t \\mid s_t)}"
  rubric:
    - "Uses a product over time."
    - "Uses target policy probability over behavior policy probability."
  explanation: "Environment dynamics cancel when both policies act in the same MDP."
  anchor: policy-ratios
- id: is-short-1
  type: short
  prompt: "Why can importance sampling have high variance in long episodes?"
  difficulty: 3
  answer: "It multiplies many probability ratios, so the product can become very large or very small."
  rubric:
    - "Mentions products over time."
    - "Mentions large weights or high variance."
  explanation: "Long products make estimates unstable."
  anchor: variance-problem
- id: is-code-1
  type: code
  lang: python
  prompt: "Fill in the importance weights."
  difficulty: 3
  scaffold: |
    import numpy as np
    p = np.array([0.8, 0.2])
    q = np.array([0.5, 0.5])
    actions = np.array([0, 1, 0])
    weights = ___
  blanks:
    - id: 1
      answer: "p[actions] / q[actions]"
      rubric:
        - "Uses target probabilities divided by behavior probabilities."
  explanation: "The importance weight is target probability over behavior probability."
  anchor: code-example
