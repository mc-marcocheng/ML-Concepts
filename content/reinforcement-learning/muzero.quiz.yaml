- id: muzero-mcq-1
  type: mcq
  prompt: "What does MuZero plan over?"
  difficulty: 3
  options:
    - "A learned latent dynamics model"
    - "Only raw pixels"
    - "Only a fixed transition table"
    - "Only random actions"
  correctIndex: 0
  explanation: "MuZero uses learned latent states for search."
  anchor: planning
- id: muzero-short-1
  type: short
  prompt: "Why does MuZero not need to reconstruct observations?"
  difficulty: 4
  answer: "It only needs latent predictions useful for rewards, values, policies, and planning."
  rubric:
    - "Mentions latent predictions."
    - "Mentions rewards, values, policies, or planning."
  explanation: "A control model need not model every observation detail."
  anchor: why-it-matters
- id: muzero-order-1
  type: order
  prompt: "Put the MuZero model functions in a typical rollout order."
  difficulty: 3
  steps:
    - "Use the representation function to make an initial latent state"
    - "Use the dynamics function with an action"
    - "Predict the next latent state and reward"
    - "Use the prediction function to get policy and value"
  explanation: "MuZero alternates latent dynamics with prediction during search."
  anchor: model-parts
- id: muzero-mcq-2
  type: mcq
  prompt: "Which search method is central to MuZero?"
  difficulty: 2
  options:
    - "Monte Carlo tree search"
    - "K-means"
    - "Beam search over tokens only"
    - "Linear regression"
  correctIndex: 0
  explanation: "MuZero uses MCTS with learned model predictions."
  anchor: planning
