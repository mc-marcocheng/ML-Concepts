- id: tp-mcq-1
  type: mcq
  prompt: "Which operation does a row-parallel linear layer usually need?"
  difficulty: 3
  options:
    - "All-reduce sum of partial outputs"
    - "Random sampling of weights"
    - "Data shuffling only"
    - "Optimizer state averaging only"
  correctIndex: 0
  explanation: "A row split produces partial outputs that must be summed across devices."
  anchor: core-idea

- id: tp-latex-1
  type: latex
  prompt: "Write the output for a column split with $W=[W_1,W_2]$."
  difficulty: 3
  answer: "[XW_1, XW_2]"
  rubric:
    - "Uses $XW_1$ and $XW_2$."
    - "Concatenates the outputs."
  explanation: "A column split partitions output features, so the outputs concatenate."
  anchor: core-idea

- id: tp-numeric-1
  type: numeric
  prompt: "A weight matrix has 8192 output columns. Four devices split columns equally. How many output columns does each device store?"
  difficulty: 1
  value: 2048
  tolerance: 0.000001
  explanation: "Each device stores 8192 divided by 4 columns."
  anchor: core-idea

- id: tp-code-1
  type: code
  lang: python
  prompt: "Fill in the column split dimension for a weight matrix shaped [in_features, out_features]."
  difficulty: 3
  scaffold: |
    import torch
    w = torch.randn(4096, 8192)
    shards = torch.chunk(w, chunks=4, dim=___)
  blanks:
    - id: 1
      answer: "1"
      rubric:
        - "Splits the output feature dimension."
  explanation: "For a column split, split the matrix along the output feature dimension."
  anchor: code-sketch
