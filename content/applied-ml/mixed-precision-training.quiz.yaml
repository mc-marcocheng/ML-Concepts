- id: mp-mcq-1
  type: mcq
  prompt: "Why does fp16 training often use loss scaling?"
  difficulty: 2
  options:
    - "To reduce gradient underflow"
    - "To remove the optimizer"
    - "To increase dataset size"
    - "To disable backpropagation"
  correctIndex: 0
  explanation: "Loss scaling makes small gradients larger during fp16 backward computation."
  anchor: loss-scaling

- id: mp-short-1
  type: short
  prompt: "Why is bf16 often more stable than fp16?"
  difficulty: 3
  answer: "bf16 has a larger exponent range than fp16, so it handles very large and very small values better."
  rubric:
    - "Mentions exponent range."
    - "Compares bf16 with fp16."
  explanation: "bf16 keeps the fp32 exponent size but uses fewer mantissa bits."
  anchor: fp16-and-bf16

- id: mp-numeric-1
  type: numeric
  prompt: "A loss is 0.25 and the loss scale is 1024. What scaled loss enters backward?"
  difficulty: 1
  value: 256
  tolerance: 0.000001
  explanation: "The scaled loss is 0.25 times 1024."
  anchor: loss-scaling

- id: mp-code-1
  type: code
  lang: python
  prompt: "Fill in the context manager for mixed precision."
  difficulty: 2
  scaffold: |
    with torch.cuda.amp.___():
        pred = model(x)
        loss = loss_fn(pred, y)
  blanks:
    - id: 1
      answer: "autocast"
      rubric:
        - "Uses autocast."
  explanation: "autocast selects lower precision for supported operations."
  anchor: code-example
