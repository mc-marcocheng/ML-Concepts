- id: gc-mcq-1
  type: mcq
  prompt: "What is the main tradeoff in gradient checkpointing?"
  difficulty: 2
  options:
    - "Less activation memory for more compute"
    - "Less compute for more labels"
    - "More optimizer states for less memory"
    - "No backward pass"
  correctIndex: 0
  explanation: "Checkpointing saves memory by recomputing forward values during backward."
  anchor: memory-and-compute-tradeoff

- id: gc-short-1
  type: short
  prompt: "What kind of memory does gradient checkpointing mainly reduce?"
  difficulty: 2
  answer: "It mainly reduces activation memory."
  rubric:
    - "Mentions activation memory."
    - "Does not claim it mainly reduces parameter memory."
  explanation: "Checkpointing stores fewer forward activations."
  anchor: core-idea

- id: gc-numeric-1
  type: numeric
  prompt: "A segment forward pass costs 40 ms. Checkpointing recomputes it once during backward. How much extra time does recomputation add?"
  difficulty: 1
  value: 40
  tolerance: 0.000001
  explanation: "One recomputation adds one extra forward cost."
  anchor: memory-and-compute-tradeoff

- id: gc-code-1
  type: code
  lang: python
  prompt: "Fill in the PyTorch checkpoint call."
  difficulty: 2
  scaffold: |
    from torch.utils.checkpoint import checkpoint

    y = ___(run_block, x)
  blanks:
    - id: 1
      answer: "checkpoint"
      rubric:
        - "Uses checkpoint on the function and input."
  explanation: "torch.utils.checkpoint.checkpoint recomputes the function during backward."
  anchor: code-example
