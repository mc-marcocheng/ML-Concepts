- id: clip-mcq-1
  type: mcq
  prompt: "When should gradient clipping happen?"
  difficulty: 2
  options:
    - "After backward and before the optimizer step"
    - "Before the forward pass only"
    - "After the optimizer step only"
    - "Only during validation"
  correctIndex: 0
  explanation: "Clipping modifies gradients before the optimizer uses them."
  anchor: code-example

- id: clip-latex-1
  type: latex
  prompt: "Write the norm clipping scale when $\\lVert g\\rVert_2 > c$."
  difficulty: 3
  answer: "\\frac{c}{\\lVert g\\rVert_2 + \\epsilon}"
  rubric:
    - "Includes the max norm c."
    - "Divides by the gradient norm."
    - "Includes epsilon or a small stabilizer."
  explanation: "The clipped gradient multiplies $g$ by this scale."
  anchor: core-idea

- id: clip-numeric-1
  type: numeric
  prompt: "A gradient norm is 10 and the max norm is 2. Ignore epsilon. What scale is applied?"
  difficulty: 1
  value: 0.2
  tolerance: 0.000001
  explanation: "The scale is 2 divided by 10."
  anchor: core-idea

- id: clip-code-1
  type: code
  lang: python
  prompt: "Fill in the PyTorch norm clipping function."
  difficulty: 2
  scaffold: |
    loss.backward()
    torch.nn.utils.___(model.parameters(), max_norm=1.0)
    optimizer.step()
  blanks:
    - id: 1
      answer: "clip_grad_norm_"
      rubric:
        - "Uses clip_grad_norm_."
  explanation: "clip_grad_norm_ clips the global gradient norm."
  anchor: code-example
