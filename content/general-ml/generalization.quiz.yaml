- id: gen-mcq-1
  type: mcq
  prompt: "What does generalization measure?"
  difficulty: 1
  options:
    - "Performance on new data"
    - "Only training loss"
    - "The number of parameters"
    - "The number of features"
  correctIndex: 0
  explanation: "Generalization measures performance on examples not used to fit the model."
  anchor: core-idea

- id: gen-latex-1
  type: latex
  prompt: "Write expected risk for loss $\\ell(f(x),y)$ under target distribution $p_{\\text{target}}$."
  difficulty: 3
  answer: "\\mathbb{E}_{(x,y)\\sim p_{\\text{target}}}[\\ell(f(x),y)]"
  rubric:
    - "Uses expectation under the target distribution."
    - "Includes the loss."
    - "Includes the model prediction and target."
  explanation: "Expected risk is the target-distribution expectation of loss."
  anchor: core-idea

- id: gen-short-1
  type: short
  prompt: "Why should the test set not guide hyperparameter tuning?"
  difficulty: 3
  answer: "Tuning on the test set leaks information and makes the final performance estimate too optimistic."
  rubric:
    - "Mentions leakage."
    - "Mentions an optimistic or biased estimate."
  explanation: "The test set should estimate final performance after model selection."
  anchor: train-validation-and-test-sets

- id: gen-mcq-2
  type: mcq
  prompt: "What can a large train-validation performance gap suggest?"
  difficulty: 2
  options:
    - "Overfitting"
    - "Perfect calibration"
    - "No labels"
    - "A smaller model always helps"
  correctIndex: 0
  explanation: "A large gap often means the model fits training data better than new data."
  anchor: generalization-gap
